{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "echo",
            "type": "shell",
            "command": "nix run .#rssg -- -d doc -o build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}